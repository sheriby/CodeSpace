#include "ptrlist.h"
#include <stdio.h>

void insertArr(int arr[], int len, List list) {
    int i;
    for (i = 0; i < len; ++i) {
        insert(arr[0], list);
    }
}

void test() {}

int main(int argc, char *argv[]) {
    test();
    return 0;
}
